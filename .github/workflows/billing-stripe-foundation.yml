name: Billing Stripe test foundation

on:
  pull_request:
    paths:
      - 'supabase/config.toml'
      - 'supabase/functions/crm-billing-checkout/**'
      - 'supabase/functions/crm-billing-portal/**'
      - 'supabase/functions/crm-billing-change-plan/**'
      - 'supabase/functions/crm-billing-schedule-change/**'
      - 'supabase/functions/stripe-billing-webhook/**'
      - 'supabase/migrations/*billing_change*'
      - 'scripts/verify-stripe-test-foundation.mjs'
      - 'scripts/verify-stripe-scheduled-changes.mjs'
      - 'docs/BILLING_STRIPE_TEST_P0_3.md'
      - 'docs/BILLING_SCHEDULED_CHANGES_P0_5.md'
      - '.github/workflows/billing-stripe-foundation.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  source-safety:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate source safety contract
        run: |
          node --check scripts/verify-stripe-test-foundation.mjs
          node scripts/verify-stripe-test-foundation.mjs
          node --check scripts/verify-stripe-scheduled-changes.mjs
          node scripts/verify-stripe-scheduled-changes.mjs

      - name: Set up Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Type-check billing Edge Functions
        run: |
          deno check --node-modules-dir=auto supabase/functions/crm-billing-checkout/index.ts
          deno check --node-modules-dir=auto supabase/functions/crm-billing-portal/index.ts
          deno check --node-modules-dir=auto supabase/functions/crm-billing-change-plan/index.ts
          deno check --node-modules-dir=auto supabase/functions/crm-billing-schedule-change/index.ts
          deno check --node-modules-dir=auto supabase/functions/stripe-billing-webhook/index.ts
